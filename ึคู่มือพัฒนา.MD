# 📘 คู่มือการพัฒนาและสถาปัตยกรรมโปรเจกต์ Portfolio (Developer Guide)
**ผู้พัฒนา:** Slithnunt Siriyong (Meen)

เอกสารฉบับนี้จัดทำขึ้นเพื่อรวบรวมซอร์สโค้ด แนวคิดการออกแบบ โครงสร้างของโปรเจกต์ และเทคนิคต่างๆ ที่ใช้ในการเขียนโค้ด เพื่อเป็นคู่มือสำหรับการพัฒนาและดูแลระบบ (Maintenance) ในอนาคต

---

## 🧠 1. แนวคิดการออกแบบและกระบวนการคิด (Design & Thought Process)

ในการ Refactor โค้ดครั้งนี้ เป้าหมายหลักคือการเปลี่ยนจาก "เว็บที่แค่ทำงานได้" ให้เป็น **"เว็บที่มีมาตรฐานระดับมืออาชีพ" (Production-Ready)** * **Design Style (Minimal & Clean):** เน้นความสะอาดตา ตัดสิ่งที่ไม่จำเป็นออก ใช้พื้นที่ว่าง (White Space) ให้เกิดประโยชน์ เพื่อให้ผลงาน (Projects) โดดเด่นที่สุด
* **Typography (ฟอนต์):** เลือกใช้ฟอนต์ **"Sarabun"** (สารบรรณ) สำหรับภาษาไทย เพราะเป็นฟอนต์มาตรฐานที่อ่านง่าย ดูเป็นทางการแต่นำสมัย และใช้ **"Source Sans Pro"** สำหรับภาษาอังกฤษ
* **Color Palette:** ใช้โทนสีน้ำเงินเป็นสีหลัก (`#0062b9`) ซึ่งให้ความรู้สึกถึงความเป็นเทคโนโลยี ความน่าเชื่อถือ (เข้ากับเอกลักษณ์ของการเรียนสาย IT/ธุรกิจ) สลับกับสีพื้นหลังโทนสว่าง (`#f4f6f8`) เพื่อแบ่งแยก Section ให้ชัดเจนโดยไม่ต้องใช้เส้นคั่น
* **Performance & SEO:** ลบ Inline Styles (`style="..."`) ออกจาก HTML ทั้งหมดเพื่อลดความซ้ำซ้อน ทำให้บราวเซอร์โหลดไฟล์ได้เร็วขึ้น และปรับโครงสร้างแท็กให้ถูกต้อง เช่น ให้มี `<h1>` แค่จุดเดียวในหน้าเว็บ (ที่ Hero Section) นอกนั้นใช้ `<h2>`, `<h3>` ตามลำดับความสำคัญ

---

## 📂 2. โครงสร้างโปรเจกต์ (Project Architecture)

โปรเจกต์นี้ใช้สถาปัตยกรรมแบบแยกส่วน (Modular) เพื่อให้ง่ายต่อการค้นหาและแก้ไขโค้ด

```text
portfolio/
├── assets/               # โฟลเดอร์หลักสำหรับเก็บไฟล์สื่อทั้งหมด
│   ├── jpeg/             # เก็บรูปถ่ายทั่วไป (เช่น M.jpg, Mexn.jpg, Meen.jpg)
│   ├── png/              # เก็บรูปโปร่งใส หรือโลโก้ต่างๆ
│   └── svg/              # เก็บไฟล์ Vector เช่น ไอคอนหรือพื้นหลัง (common-bg.svg)
├── css/
│   └── style.css         # ไฟล์ CSS ที่ถูก Compile แล้ว (ไฟล์นี้เชื่อมกับ HTML)
├── scss/                 # ซอร์สโค้ด SCSS ที่แบ่งเป็นหมวดหมู่ (7-1 Pattern)
│   ├── main.scss         # ไฟล์ศูนย์รวม (Import ไฟล์อื่นๆ มารวมกัน)
│   ├── abstracts/        # ตัวแปร (Variables) และ Mixins
│   ├── base/             # การตั้งค่าพื้นฐานของ HTML/Body
│   ├── components/       # ส่วนประกอบย่อย (ปุ่ม, Navbar, Footer)
│   └── pages/            # สไตล์เฉพาะของแต่ละหน้า/Section
├── index.html            # ไฟล์โครงสร้างเว็บหลัก (Semantic HTML)
└── index.js              # สคริปต์ควบคุมการทำงาน (เช่น การเปิด/ปิด Mobile Menu)

🛡️ 3. เทคนิคที่ใช้และ Best Practices (Technical Decisions)
CSS Variables (:root): แทนที่จะจำรหัสสี #0062b9 ไปใส่ทุกที่ เราประกาศตัวแปร --themeClrPrimary ไว้ที่บนสุด หากในอนาคตต้องการเปลี่ยนตีมเว็บเป็นสีแดง ก็แค่แก้ค่าตัวแปรนี้จุดเดียว สีจะเปลี่ยนทั้งเว็บ

Security Enhancement (ความปลอดภัย):
ในทุกลิงก์ที่กดแล้วเปิดหน้าต่างใหม่ (target="_blank") เช่น ลิงก์ไป Facebook หรือ Github จะมีการใส่ rel="noopener noreferrer" เสมอ เพื่อป้องกันการโจมตีแบบ Reverse Tabnabbing (แฮกเกอร์ใช้หน้าต่างใหม่ย้อนกลับมาเปลี่ยน URL หน้าเดิมของเราเพื่อดักจับข้อมูล)

REM & Responsive Scaling:
เราใช้เทคนิคกำหนด html { font-size: 62.5%; } เพื่อแปลงให้ 1rem = 10px ทำให้การกะระยะง่ายขึ้น (เช่น padding: 2rem = 20px) และเมื่อเปิดในมือถือ เราแค่ลดเปอร์เซ็นต์นี้ลง ขนาดของทุกอย่างในเว็บจะย่อส่วนลงอย่างสมส่วนโดยอัตโนมัติ

Asset Linking (การเชื่อมโยงรูปภาพ):

ใน index.html เรียกใช้รูปภาพด้วย src="./assets/jpeg/ชื่อรูป.jpg"

ใน style.css เรียกใช้ภาพพื้นหลังด้วย url('../assets/svg/ชื่อรูป.svg') (ต้องถอยหลัง 1 โฟลเดอร์ ../ เพราะไฟล์ CSS อยู่ในโฟลเดอร์ css/)

🚀 4. คู่มือการพัฒนาต่อ (How to Continue Developing)
หากต้องการเพิ่มโปรเจกต์ใหม่: ให้ไปที่ไฟล์ index.html เลื่อนหาแท็ก <section id="projects"> และคัดลอกบล็อก <div class="projects__row text-center-block"> ... </div> มาวางต่อกัน เปลี่ยนข้อความและลิงก์ได้เลย รูปแบบจะจัดเรียงตรงกลางอัตโนมัติ

หากต้องการแก้ไขสีตีม:
ไปที่ไฟล์ css/style.css (หรือ scss/abstracts/_variables.scss ถ้าใช้ SCSS) เลื่อนไปที่บรรทัดบนสุดตรง :root และแก้ไขตัวแปร --themeClrPrimary

คำแนะนำสำหรับ SCSS:
หากคุณต้องการเขียนด้วย SCSS ต่อ แนะนำให้ติดตั้ง Extension ใน VS Code ชื่อ "Live Sass Compiler" เมื่อคุณแก้ไขไฟล์ในโฟลเดอร์ scss/ แล้วกด Watch ระบบจะแปลงไฟล์ไปทับ css/style.css ให้อัตโนมัติ